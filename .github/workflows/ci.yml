name: CI Pipeline (Delivery)

on:
  push:
    branches:
      - main   # ✅ ONLY main branch triggers delivery

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # -------------------------
      # Checkout & Setup
      # -------------------------
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm ci || npm install

      # -------------------------
      # Run Tests (DO NOT EXIT JOB)
      # -------------------------
      - name: Run tests
        id: tests
        continue-on-error: true
        run: npm test

      # -------------------------
      # Extract Jira Issue Key
      # -------------------------
      - name: Extract Jira issue key
        id: jira
        run: |
          ISSUE_KEY=$(git log --pretty=%B | grep -oE '[A-Z]+-[0-9]+' | head -n 1 || true)
          echo "issue_key=$ISSUE_KEY" >> $GITHUB_OUTPUT
          echo "Detected Jira issue: $ISSUE_KEY"

      # -------------------------
      # Jira Comment – SUCCESS
      # -------------------------
      - name: Comment on Jira (Delivery success)
        if: github.ref == 'refs/heads/main' && steps.tests.outcome == 'success' && steps.jira.outputs.issue_key != ''
        run: |
          curl -X POST \
            -u "${{ secrets.JIRA_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}" \
            -H "Content-Type: application/json" \
            https://${{ secrets.JIRA_DOMAIN }}/rest/api/3/issue/${{ steps.jira.outputs.issue_key }}/comment \
            -d '{
              "body": {
                "type": "doc",
                "version": 1,
                "content": [
                  {
                    "type": "paragraph",
                    "content": [
                      {
                        "type": "text",
                        "text": "✅ Delivery CI passed. Change has been successfully merged into main."
                      }
                    ]
                  }
                ]
              }
            }'

      # -------------------------
      # Jira Comment – FAILURE
      # -------------------------
      - name: Comment on Jira (Delivery failure)
        if: github.ref == 'refs/heads/main' && steps.tests.outcome == 'failure' && steps.jira.outputs.issue_key != ''
        run: |
          curl -X POST \
            -u "${{ secrets.JIRA_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}" \
            -H "Content-Type: application/json" \
            https://${{ secrets.JIRA_DOMAIN }}/rest/api/3/issue/${{ steps.jira.outputs.issue_key }}/comment \
            -d '{
              "body": {
                "type": "doc",
                "version": 1,
                "content": [
                  {
                    "type": "paragraph",
                    "content": [
                      {
                        "type": "text",
                        "text": "❌ Delivery CI failed on main. Issue cannot be marked as complete."
                      }
                    ]
                  }
                ]
              }
            }'

      # -------------------------
      # Jira Transition – SUCCESS → Done
      # -------------------------
      - name: Move Jira issue to Done
        if: github.ref == 'refs/heads/main' && steps.tests.outcome == 'success' && steps.jira.outputs.issue_key != ''
        run: |
          curl -X POST \
            -u "${{ secrets.JIRA_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}" \
            -H "Content-Type: application/json" \
            https://${{ secrets.JIRA_DOMAIN }}/rest/api/3/issue/${{ steps.jira.outputs.issue_key }}/transitions \
            -d '{
              "transition": {
                "id": "${{ secrets.JIRA_DONE_TRANSITION_ID }}"
              }
            }'

      # -------------------------
      # Jira Transition – FAILURE → In Progress
      # -------------------------
      - name: Move Jira issue to In Progress
        if: github.ref == 'refs/heads/main' && steps.tests.outcome == 'failure' && steps.jira.outputs.issue_key != ''
        run: |
          curl -X POST \
            -u "${{ secrets.JIRA_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}" \
            -H "Content-Type: application/json" \
            https://${{ secrets.JIRA_DOMAIN }}/rest/api/3/issue/${{ steps.jira.outputs.issue_key }}/transitions \
            -d '{
              "transition": {
                "id": "${{ secrets.JIRA_IN_PROGRESS_TRANSITION_ID }}"
              }
            }'

      # -------------------------
      # FINAL: Fail CI if Tests Failed
      # -------------------------
      - name: Fail CI if tests failed
        if: steps.tests.outcome == 'failure'
        run: |
          echo "Tests failed on main. Marking CI job as failed."
          exit 1
